# 周报生成器 Skill

[根目录](../../CLAUDE.md) > [skills](../) > **weekly-report-generator**

> 最后更新：2026-01-15 23:20:35

## 模块职责

周报生成器是一个自动化工具，能够从 Git 提交记录生成专业的工作周报文档。

**核心功能**：
- 灵活的时间表达式解析（本周、上周、自定义日期范围等）
- 获取 Git 提交日志
- AI 智能清洗技术术语为业务语言
- 按周一到周五分组组织内容
- 生成专业格式的周报文档
- 支持基于用户示例风格定制输出

**适用场景**：
1. 从 Git 历史生成周报
2. 将提交记录转换为领导汇报风格
3. 多项目汇总周报
4. 基于用户示例风格生成定制化周报

## 入口与启动

### 调用方式

该 skill 通过两个 Python 脚本实现，可以独立调用或组合使用：

**脚本 1：获取 Git 日志**
```bash
python scripts/get_git_logs.py \
  --paths "项目路径1,项目路径2" \
  --since "2025-01-13" \
  --until "2025-01-17"
```

**脚本 2：导出周报**
```bash
python scripts/export_report.py \
  --content "清洗后的周报内容" \
  --output "输出路径" \
  --filename "20250115周报.md" \
  --start-date "2025-01-13" \
  --end-date "2025-01-17" \
  --total-commits 15
```

### 工作流程（5 步）

完整的周报生成流程遵循 5 个步骤：

#### Step 1: 收集用户输入

需要收集以下信息：

1. **项目路径**（必需）
   - 单个项目：`E:\工作\study\myproject`
   - 多个项目：`E:\proj1, E:\proj2, E:\proj3`

2. **时间表达式**（必需）
   - 相对时间：`本周`、`上周`、`本月`
   - 绝对时间：`2025.1.10 - 2025.1.15`
   - 混合时间：`上周三到今天`

3. **示例周报**（可选，推荐）

   **方式1：直接粘贴文本内容**
   - 用户直接粘贴示例周报文本
   - AI 将模仿文本格式和风格

   **方式2：提供模板文件路径**（推荐）
   - 用户提供示例周报文件路径（.md 或 .docx）
   - 调用 `analyze_template.py` 分析模板结构
   - AI 基于结构化数据精确模仿

   AI 将严格模仿示例的：
   - 标题样式和层级
   - 章节结构和顺序
   - 语言风格和术语使用
   - 格式和排版

4. **输出文件名**（必需）
   - 用户指定输出文件名
   - 如：`20250115周报.md`

#### Step 2: AI 解析时间 + 交互确认

AI 解析时间表达式，计算对应的日期范围：

**解析规则**：
- 相对时间对齐到工作周（周一至周五）
- 绝对时间保持原样
- 混合时间计算实际日期范围

**示例**：
- `本周` → 2025年1月13日（周一）至2025年1月17日（周五）
- `上周` → 2025年1月6日（周一）至2025年1月10日（周五）
- `2025.1.10 - 2025.1.15` → 2025年1月10日至2025年1月15日

**交互确认**：
```
AI: "本周 = 2025年1月13日（周一）至2025年1月17日（周五），确认吗？"
用户: "确认" 或 "调整日期"
```

#### Step 3: 获取 Git 日志

调用 `scripts/get_git_logs.py` 获取提交记录。

#### Step 3.5: 分析示例模板（如果用户提供）

调用 `scripts/analyze_template.py` 分析模板文件，提取结构化信息。

#### Step 4: AI 清洗内容 + 生成周报

AI 基于 `references/report-prompts.md` 的规则清洗内容，并基于模板结构（如果提供）生成周报。

#### Step 5: 输出文件

调用 `scripts/export_report.py` 生成文件。

## 对外接口

### get_git_logs.py

**功能**：获取 Git 日志并按周一到周五分组

**参数**：
- `--paths`（必需）：项目路径列表，用逗号分隔
- `--since`（必需）：开始日期（YYYY-MM-DD 格式）
- `--until`（必需）：结束日期（YYYY-MM-DD 格式）

**返回格式**（JSON）：
```json
{
  "start_date": "2025-01-13",
  "end_date": "2025-01-17",
  "total_commits": 15,
  "commits_by_day": {
    "周一": [
      {
        "hash": "abc123",
        "author": "张三",
        "email": "zhangsan@example.com",
        "message": "修复登录bug",
        "date": "2025-01-13",
        "time": "10:30",
        "weekday": "周一"
      }
    ],
    "周二": [...],
    "周三": [],
    "周四": [...],
    "周五": [...]
  }
}
```

**特性**：
- 只返回工作日（周一到周五）的提交
- 支持多项目聚合
- 按天分组
- 自动跳过周末的提交
- 输入验证：日期格式、路径有效性检查

### analyze_template.py

**功能**：分析周报模板文件（Markdown 或 Word），提取结构化信息供 AI 使用

**参数**：
- `--template`（必需）：模板文件路径（支持 .md、.docx）
- `--output`（可选）：输出 JSON 文件路径（不提供则打印到控制台）

**返回格式**（JSON）：
```json
{
  "type": "markdown",
  "structure": {
    "title": "模板标题",
    "sections": [
      {
        "level": 1,
        "title": "章节标题",
        "content": "章节内容预览"
      }
    ]
  },
  "variables": {
    "{{变量名}}": {
      "name": "变量名",
      "description": "变量：变量名",
      "required": true
    }
  },
  "raw_content": "模板的完整原始内容"
}
```

**使用示例**：
```bash
# 分析 Markdown 模板
python scripts/analyze_template.py \
  --template "/path/to/template.md" \
  --output "template_structure.json"

# 分析 Word 模板
python scripts/analyze_template.py \
  --template "/path/to/template.docx"
```

**支持的功能**：
- ✅ 自动检测文件类型（.md / .docx）
- ✅ 提取标题层级结构（#、##、###）
- ✅ 识别变量占位符（`{{变量名}}`）
- ✅ 返回完整的原始内容
- ✅ 判断变量是否必需
- ✅ 专业的 Word 文档解析（提取标题样式、层级等）

**典型工作流**：
1. 用户提供示例周报文件
2. 调用 `analyze_template.py` 获取模板结构
3. AI 基于结构信息精确模仿格式
4. 生成的新周报严格遵循模板风格

### export_report.py

**功能**：将 AI 生成的周报内容写入文件，支持 Markdown、Word 格式

**参数**：
- `--content`（必需）：完整的周报内容（AI 已生成，Markdown 格式）
- `--output`（必需）：输出目录路径
- `--filename`（可选）：输出文件名（如：20250115周报.md，不提供则自动生成）
- `--start-date`（可选）：开始日期（YYYY-MM-DD 格式，用于自动文件名）
- `--end-date`（可选）：结束日期（YYYY-MM-DD 格式，用于自动文件名）

**自动格式检测**：
- **根据 `filename` 扩展名自动检测输出格式**：
  - `.md` → Markdown 格式
  - `.docx` → Word 格式
  - 未指定或无扩展名 → 默认 Markdown 格式
- **无需 `--format` 参数**：脚本自动根据文件名选择输出方式

**自动文件名生成**：
- 格式：`周报YYYYMMDD-YYYYMMDD.md`
- 示例：`周报20250113-20250117.md`
- 触发条件：未提供 `--filename` 时，必须提供 `--start-date` 和 `--end-date`

**使用示例**：
```bash
# Markdown 输出
python scripts/export_report.py \
  --content "AI生成的完整周报内容" \
  --output "output" \
  --filename "周报.md"

# Word 输出（自动检测 .docx 扩展名）
python3.12 scripts/export_report.py \
  --content "AI生成的完整周报内容" \
  --output "output" \
  --filename "周报.docx"

# 自动文件名（默认 Markdown）
python3.12 scripts/export_report.py \
  --content "AI生成的完整周报内容" \
  --output "output" \
  --start-date "2025-01-13" \
  --end-date "2025-01-17"
# 生成：output/周报20250113-20250117.md
```

## 关键依赖与配置

### 依赖清单

**requirements.txt**：
```
gitpython>=3.1.40
python-docx>=0.8.11
```

**依赖说明**：
- `gitpython>=3.1.40` - Git 操作库，用于读取 Git 提交记录
- `python-docx>=0.8.11` - Word 文件读写库（可选，仅 Word 输出时需要）

**环境要求**：
- Python 3.8+ （推荐 Python 3.12）
- Git（用于访问 Git 仓库）

**安装方法**：
```bash
# 使用 requirements.txt
pip install -r requirements.txt

# 或直接安装
pip install gitpython python-docx
```

## 数据模型

### 输入数据

**时间表达式**：
- 相对时间：`本周`、`上周`、`本月`
- 绝对时间：`2025.1.10 - 2025.1.15`
- 混合时间：`上周三到今天`

**项目路径**：
- 单个路径：`E:\工作\study\myproject`
- 多个路径：`E:\proj1, E:\proj2, E:\proj3`

**示例周报**（可选）：
- 用户提供一个示例周报文本
- AI 将模仿其格式和风格

### 输出数据

**周报文件**：
- 格式：Markdown (.md)
- 内容：格式化的工作周报
- 结构：标题、汇报周期、工作内容、问题和风险、提交统计

### 数据处理流程

```
Git 提交记录
    ↓
按工作日分组（周一到周五）
    ↓
AI 清洗内容
    ↓
转换为业务语言
    ↓
填充模板
    ↓
生成 Markdown 文件
```

## 测试与质量

### 测试方法

**手动测试流程**：

1. **测试单个项目**
   ```bash
   python scripts/get_git_logs.py \
     --paths "E:\工作\study\test-project" \
     --since "2025-01-13" \
     --until "2025-01-17"
   ```

2. **测试多项目聚合**
   ```bash
   python scripts/get_git_logs.py \
     --paths "E:\proj1,E:\proj2,E:\proj3" \
     --since "2025-01-13" \
     --until "2025-01-17"
   ```

3. **测试模板渲染**
   ```bash
   python scripts/export_report.py \
     --content "1. 完成用户管理功能\n2. 优化登录性能" \
     --output "E:\工作\study\weekly-report\output" \
     --filename "test.md" \
     --start-date "2025-01-13" \
     --end-date "2025-01-17" \
     --total-commits 10
   ```

### 质量保证

**代码质量**：
- 遵循 PEP 8 编码规范
- 使用类型注解（Type Hints）
- 完整的函数文档字符串
- 错误处理和异常捕获

**输出质量**：
- AI 严格遵循术语转换规则
- 业务语言而非技术术语
- 条目控制在 20 条以内
- 同类工作合并，避免碎片化

## AI 内容清洗规则

### 内容提炼原则

1. **聚焦业务价值**：不罗列代码细节，提炼"做了什么、解决了什么、带来什么效果"
2. **同类合并**：多次小修复整合为一条，避免碎片化
3. **优先级筛选**：优先功能开发、核心优化、重要修复
4. **条目控制**：控制在 20 条以内

### 语言风格

- 正式简洁，符合技术岗汇报语境
- 禁止套话："综上所述""总而言之""助力""赋能"等
- 禁止动作描述："我提交了""我修改了""提交代码"
- 每条以动词开头："新增""完善""优化""修复""调整""支持"

### 术语转换要求（重要）

| 禁止使用 | 应转换为 |
|---------|---------|
| API、接口 | 数据通道、数据对接、系统对接 |
| 前端、后端、服务端 | 页面端、系统端、服务层 |
| 组件、控件 | 功能模块、页面元素、操作区域 |
| 字段、参数 | 信息项、数据项、配置项 |
| 数据库、表 | 数据存储、数据记录 |
| 缓存、Redis | 数据缓冲、临时存储 |
| 接口响应、请求 | 系统响应、数据交互 |
| bug、issue | 问题、异常、故障 |
| 重构、封装 | 结构优化、逻辑整理 |
| 渲染、样式 | 显示效果、界面展示 |
| 联调、部署 | 系统对接测试、系统上线 |
| 分支、合并、commit | （不体现，直接描述工作内容） |
| JSON、XML、格式 | 数据格式、数据结构 |
| 权限校验、鉴权 | 权限控制、访问管理 |
| 异步、同步、并发 | （不体现，描述业务效果） |

**示例转换**：

输入（Git 提交信息）：
```
- 修复仪表盘数据统计错误
- 仪表盘新增项目个数字段
- 仪表盘图表渲染异常
- 表格组件支持提示文本颜色配置
- 合同管理增加信用代码字段
```

输出（清洗后）：
```markdown
1. 完善仪表盘统计功能，新增项目数量展示，修复数据统计及图表显示异常问题
2. 优化表格展示效果，支持提示文本颜色自定义配置
3. 扩展合同管理模块，增加企业信用代码信息项
```

## 常见问题（FAQ）

### Q1: 如何处理非 Git 目录？

**A**: 脚本会自动检测并跳过非 Git 目录，输出警告信息：
```
警告：E:\not-a-git-repo 不是Git仓库，跳过
```

### Q2: 周末的提交会被过滤吗？

**A**: 是的，脚本只会保留周一到周五的提交，周六和周日的提交会被自动过滤。

### Q3: 如何自定义周报模板？

**A**: 有两种方式：
1. 在调用 `export_report.py` 时使用 `--template` 参数提供自定义模板
2. 在使用 skill 时提供示例周报，AI 会严格模仿示例的格式和风格

### Q4: 支持哪些时间表达式？

**A**: 支持：
- 相对时间：`本周`、`上周`、`本月`
- 绝对时间：`2025.1.10 - 2025.1.15`
- 混合时间：`上周三到今天`

AI 会解析并计算实际的日期范围，然后交互确认。

### Q5: 如何处理多项目汇总？

**A**: 使用逗号分隔多个项目路径：
```bash
--paths "E:\proj1,E:\proj2,E:\proj3"
```

脚本会自动聚合所有项目的提交记录，并按天分组。

### Q6: 输出文件格式是什么？

**A**: 输出 Markdown 格式（.md），可以：
- 直接在 Markdown 编辑器中查看
- 转换为 Word 等格式
- 在支持 Markdown 的平台直接使用

### Q7: 如何确保周报内容符合领导要求？

**A**:
1. 提供示例周报，AI 会严格模仿示例的格式和风格
2. AI 会自动将技术术语转换为业务语言
3. AI 会合并同类工作，避免碎片化
4. 生成后可以手动微调

## 相关文件清单

### 核心文件

| 文件路径 | 说明 |
|---------|------|
| `SKILL.md` | Skill 核心文档，详细说明功能和使用方法 |
| `CLAUDE.md` | 本文档，AI 上下文和实现细节 |
| `requirements.txt` | Python 依赖列表 |

### 脚本文件

| 文件路径 | 说明 |
|---------|------|
| `scripts/get_git_logs.py` | 获取 Git 日志并按工作日分组 |
| `scripts/export_report.py` | 根据模板导出周报文件 |

### 资源文件

| 文件路径 | 说明 |
|---------|------|
| `references/report-prompts.md` | AI 内容清洗规则和术语转换表 |

## 变更记录

### 2026-01-15 23:20:35
- 更新时间戳
- 添加导航面包屑
- 完善文档内容和结构
- 确保与根级文档的一致性

### 2026-01-15 21:58:03
- 创建模块级 CLAUDE.md 文档
- 添加导航面包屑
- 详细描述模块实现细节
- 添加接口说明、数据模型、测试方法
- 添加 FAQ 和相关文件清单
